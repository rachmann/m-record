<Window x:Class="m_record.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="350" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <Grid Margin="20">

        <StackPanel Margin="20,0,10,20" Grid.Row="0">
            <CheckBox x:Name="DarkModeCheckBox" Content="Enable Dark Mode" />
        </StackPanel>
        <StackPanel Margin="20,0,10,20" Grid.Row="1">
            <!-- ...existing controls... -->
            <TextBlock Text="Notification Style:" Margin="0,10,0,0"/>
            <ComboBox x:Name="NotifyComboBox" Margin="0,6,0,0" Width="200">
                <ComboBoxItem Content="None" />
                <ComboBoxItem Content="MessageOnly" />
                <ComboBoxItem Content="NotificationArea" />
                <ComboBoxItem Content="Both" />
            </ComboBox>

            <!-- ...existing buttons... -->
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3">
            <Button Content="OK" Width="60" Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="60" Click="CancelButton_Click"/>
        </StackPanel>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="24" />
        </Grid.RowDefinitions>
    </Grid>
</Window>